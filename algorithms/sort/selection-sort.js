
/**
 * Selection sort - sort a data list using input function
 *
 * @param {Array} input an unsorted array of item
 * @param {Function} compare a function to compare 2 items
 * @returns {Array} a sorted array of number
 */
function selectionSort (input, compare) {

}

module.exports = {
  selectionSort
}
